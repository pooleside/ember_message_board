<div class="container">
 <div class="well">
   <h3>{{question.title}}</h3>
   <h5>{{question.author}}</h5>
   <p>{{question.body}}</p>
   {{update-question question=question update="update"}} <br>
   <button{{action 'delete' question}} class= "btn btn-xs btn-default">Delete</button>
 </div>

 <hr>

{{new-answer saveAnswer="saveAnswer" question=question}}

<hr>
{{#each question.answers as |answer|}}
  <div class="well">
      <strong>{{answer.user}}</strong>
      <br>
      {{answer.text}}
      <br><br>
  </div>
{{/each}}

</div>
<!--<div class="row emphasis">Q: {{question.summary}}</div>

<div class="row">
  <h5>asked by {{question.author}}</h5>
</div>

<div class="row">
  <p>{{question.title}}</p>
</div>

<div class="row">
  <h5>{{question.body}}</h5>
</div>

<div class="row">{{new-answer saveAnswer="saveAnswer" question=question}}</div>

<div class="row">
  <ul>
    {{#each question.answers as |answer|}}
      <span class="minoremphasis">A: by {{answer.user}}</span>
      <li class="answer">{{answer.text}}</li>
      <div class="pointer right icon" {{action 'deleteAnswer' answer}}>
        <span class="glyphicon glyphicon-trash"></span></div>
    {{/each}}
  </ul>
</div> -->
