import Ember from 'ember';

export default Ember.Component.extend({
  //actions: {
    //createQuestion(post) {
      //this.sendAction('createQuestion', question);
      //this.set('question', {});
    //}
  //}
});
